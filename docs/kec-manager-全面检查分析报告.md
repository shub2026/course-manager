# KEC 课程管理平台 — 全面检查分析报告

> 分析日期：2026-06-11 | 仓库：github.com/shub2026/kec-manager | 分支：main | 最新提交：e4551f0

---

## 一、项目概览

| 维度 | 详情 |
|------|------|
| **项目名称** | KEC 课程管理平台 (course-management) |
| **定位** | 面向职业技术院校的轻量级课程管理平台 |
| **架构** | 前后端分离：Vue 3 + Express + Prisma + SQLite |
| **代码规模** | 前端 ~8500+ 行，后端 28 个源文件，12 个 Prisma 数据模型 |
| **文档规模** | 12 份文档（含 2 份审计报告），README 完整详尽 |
| **提交历史** | 20+ 次提交，含多次 Bug 修复和功能迭代 |
| **代码质量** | 整体良好，存在若干关键安全问题待修复 |

---

## 二、技术栈评估

| 层级 | 技术 | 版本 | 评价 |
|------|------|------|------|
| 前端框架 | Vue 3 (Composition API) | 3.5.34 | ✅ 现代写法，`<script setup>` 一致使用 |
| UI 组件库 | Element Plus | 2.14.1 | ✅ 企业级组件，中文国际化适配良好 |
| 构建工具 | Vite | 5.4.21 | ✅ 极速 HMR，配置简洁 |
| 状态管理 | Pinia | 3.0.4 | ✅ Vue 3 官方推荐，使用得当 |
| 后端框架 | Express | 5.1.0 | ✅ ESM 模块化，结构清晰 |
| ORM | Prisma | 6.19.3 | ✅ 类型安全，支持 SQLite/MySQL 切换 |
| 认证 | JWT (jsonwebtoken) | 9.0.3 | ⚠️ 双 Token 机制但共享同一密钥 |
| 密码加密 | bcryptjs | 3.0.3 | ⚠️ 迭代次数 10（推荐 ≥12） |
| Excel | ExcelJS | 4.4.0 | ✅ 纯 JS 实现，无系统依赖 |
| 日志 | Winston | 3.19.0 | ⚠️ 已配置但代码中几乎未使用 |

---

## 三、安全问题分析

### 🔴 严重（5 项）

| 编号 | 问题 | 位置 | 影响 |
|------|------|------|------|
| **C1** | JWT 密钥明文存储于 `.env`（已修复） | `server/.env` | 若该文件被版本控制或泄露，所有 Token 可被伪造 |
| **C2** | 登录接口无速率限制（已修复） | `server/src/routes/auth.routes.js:9` | 可被暴力破解密码 |
| **C3** | Token 支持 URL 查询参数传递 | `server/src/middleware/auth.middleware.js:12-13` | Token 会被记录在服务器日志、浏览器历史、Referer 头中 |
| **C4** | 班级 POST/PUT/DELETE 缺少角色权限校验 | `server/src/routes/class.routes.js` | 访客角色可创建/修改/删除班级 |
| **C5** | 培养方案 POST/PUT/DELETE 缺少角色权限校验 | `server/src/routes/plan.routes.js` | 访客角色可创建/修改/删除培养方案 |

### 🟠 高危（8 项）

| 编号 | 问题 | 位置 | 影响 |
|------|------|------|------|
| H1 | 错误处理器泄露内部错误信息 | `server/src/middleware/error.js:5-9` | 生产环境暴露数据库细节 |
| H2 | 自定义错误类定义但从未使用 | `server/src/utils/error.js` | 无法区分操作错误和程序错误 |
| H3 | 修改密码接口未应用密码强度校验中间件 | `server/src/routes/auth.routes.js:78-95` | 允许弱密码如 "aaaaaaaa" |
| H4 | 创建用户时不校验密码强度 | `server/src/routes/user.routes.js:53` | 管理员可创建极弱密码用户 |
| H5 | 系统设置 PUT 接口可注入任意 Key | `server/src/routes/settings.routes.js:35-46` | 可破坏学期配置导致全站异常 |
| H6 | 批量导入非事务性操作 | `server/src/routes/import.routes.js` | 部分成功部分失败，无回滚机制 |
| H7 | 导入数据未经清洗直接存储 | `server/src/routes/import.routes.js:221` | 潜在存储型 XSS |
| H8 | 直接修改 Prisma 查询结果对象 | `server/src/routes/class.routes.js:260-267` | 代码异味，可能导致非预期行为 |

### 🟡 中危（12 项）

| 编号 | 问题 |
|------|------|
| M1 | 路由错误处理模式不一致（3 种不同方式） |
| M2 | 审计日志记录方式不一致（直接调用 vs Service） |
| M3 | 学期参数解析逻辑重复 5 次 |
| M4 | 方案匹配逻辑重复实现多次 |
| M5 | GET 请求中执行写操作（排序自动修复） |
| M6 | `getActiveClassFilter` 无 duration 空值兜底 |
| M7 | 生产代码中大量 `console.log` 调试输出 |
| M8 | PUT 路由中字段可能传 `undefined` |
| M9 | bcrypt 迭代次数仅 10 |
| M10 | Access Token 和 Refresh Token 共用同一密钥 |
| M11 | 系统设置 GET 接口执行写操作（创建缺失设置） |
| M12 | 导入路由内置调试中间件 |

### 🔵 低危（12 项）

主要包括：未使用的工具函数/常量、日志器未使用、CSS 重复、无障碍缺失、无国际化支持、关闭时未等待活跃连接等。

---

## 四、前端代码分析

### 🔴 严重（3 项）

| 编号 | 问题 | 位置 |
|------|------|------|
| FC1 | 导入错误对话框使用 `dangerouslyUseHTMLString` | `ClassList.vue:893-907` |
| FC2 | Token 通过 URL 查询参数暴露 | `SemesterQuery.vue:150`, `HistoricalSemesterQuery.vue:201` |
| FC3 | 生产代码残留调试 `console.log`（含敏感信息） | `ClassList.vue:784-795` |

### 🟠 高危（6 项）

| 编号 | 问题 |
|------|------|
| FH1 | API 响应数据访问方式不一致（`res.data` vs `response.data`） |
| FH2 | 删除操作缺少组件级错误处理 |
| FH3 | 批量操作使用逐条 API 调用而非批量接口 |
| FH4 | Token 刷新队列存在竞态条件 |
| FH5 | 无 CSRF 保护（部分端点使用 cookie） |
| FH6 | 开发模式暴露测试账号密码 |

### 🟡 中危（9 项）

包括：表单验证不一致、排序交换竞态、`sort_order`/`sortOrder` 命名混用、缓存无大小限制、导出实现模式不一致、缺少加载状态、UserManagement 字段命名 Bug、PlanList 筛选非响应式、上传绕过 Axios 拦截器。

### 🔵 低危（10 项）

包括：无障碍缺失、无国际化、空函数残留、CSS 重复、版权信息含个人邮箱、settings store 无格式校验等。

---

## 五、文档与配置分析

> 文档省略

## 六、代码架构评价

### ✅ 优点

1. **前后端分离架构清晰**：14 个路由模块、9 个 API 模块，职责分明
2. **命名转换中间件设计优雅**：`snake_case` ↔ `camelCase` 自动转换，解耦前后端
3. **Token 刷新队列机制**：`failedQueue` 模式防止并发刷新
4. **Prisma ORM 使用得当**：有效防止 SQL 注入，支持数据库切换
5. **课程矩阵组件设计良好**：`CourseMatrix.vue` 作为可复用组件，props/emits/expose 完整
6. **数据重置功能设计周全**：三级级联策略 + "输入确认" 防止误操作
7. **README 文档详尽**：覆盖快速开始、API 文档、部署指南、FAQ
8. **JWT 密钥启动校验**：缺失时立即报错，防止不安全运行

### ⚠️ 需改进

1. **权限校验不完整**：class 和 plan 的写操作缺少角色中间件
2. **大量死代码**：错误类、验证器、常量、审计中间件等已定义但未集成
3. **日志系统虚设**：Winston 已配置但全项目仍用 `console.log`
4. **导入操作非事务性**：大数据量导入缺乏原子性保证
5. **文档与代码命名不一致**：camelCase vs snake_case 是全局性问题

---

## 七、修复优先级建议

### 第一优先级（立即修复）

| 序号 | 问题 | 预计工时 |
|------|------|----------|
| 1 | 为 class.routes.js 和 plan.routes.js 添加 `roleMiddleware('admin')` | 0.5h |
| 2 | 为登录接口添加 `express-rate-limit` | 0.5h |
| 3 | 确保 `.env` 不被版本控制追踪 | 0.1h |
| 4 | 移除 Token URL 查询参数支持 | 0.5h |
| 5 | 修复 `update-class-status.js` Prisma 模型名 | 0.1h |

### 第二优先级（本周内）

| 序号 | 问题 | 预计工时 |
|------|------|----------|
| 6 | 错误处理器区分生产和开发环境 | 0.5h |
| 7 | 修改密码接口应用 `validateChangePassword` 中间件 | 0.3h |
| 8 | 创建用户时校验密码强度 | 0.3h |
| 9 | 系统设置 PUT 添加 Key 白名单 | 0.5h |
| 10 | 导入操作包装为事务 | 2h |
| 11 | 移除生产代码中的调试 console.log | 1h |
| 12 | 前端导出改用 fetch + blob（不用 window.open） | 1h |

### 第三优先级（迭代优化）

| 序号 | 问题 | 预计工时 |
|------|------|----------|
| 13 | 统一错误处理模式 | 2h |
| 14 | 整合自定义错误类到路由 | 3h |
| 15 | 启用 Winston 日志替换 console | 2h |
| 16 | 提取重复逻辑为共享工具函数 | 2h |
| 17 | 统一审计日志记录方式 | 1h |
| 18 | 增加 bcrypt 迭代次数至 12 | 0.2h |
| 19 | Access/Refresh Token 使用不同密钥 | 1h |
| 20 | 前端 UserManagement 字段命名修复 | 0.5h |

---

## 八、统计数据总览

| 维度 | 严重 | 高危 | 中危 | 低危 | 合计 |
|------|------|------|------|------|------|
| 后端安全 | 5 | 8 | 12 | 12 | 37 |
| 前端代码 | 3 | 6 | 9 | 10 | 28 |
| 文档配置 | 3 | 0 | 0 | 0 | 3 |
| **总计** | **11** | **14** | **21** | **22** | **68** |

---

## 九、结论

KEC 课程管理平台是一个**架构设计良好、功能完整**的教学管理系统，技术选型合理，代码组织清晰。项目经过多轮审计和修复，整体质量已有明显提升。

当前最紧迫的问题是 **5 项严重安全漏洞**（权限校验缺失、登录无速率限制、Token 泄露风险），以及 **`update-class-status.js` 脚本的致命 Bug**。这些问题修复成本低（合计约 2 个工时），但影响面广，建议优先处理。

文档方面，12 份文档覆盖面广，但 **camelCase/snake_case 命名不一致** 是全局性问题，可能导致新开发者困惑。建议在后续文档中统一使用与实际代码一致的 snake_case 命名。

---

*报告由自动化代码审查生成，共分析 68 个源文件、12 份文档、12 个数据模型。*
